#include "main.h"
/**
 * _abs - a funtion that computes the absolute value of a number
 *
 * @n: is the integer parameter to be computed
 *
 * Return: return the absolute value of the given number
 */
int _abs(int n)
{
	if (n > 0)
		return (n);

	else
		return (-n);

}
